# 目录标题等级划分：为什么让 LLM 返回 父标题 比直接返回 标题等级数值 更准确

<p align="left">
  <b>核心问题：标题等级不是标题自身的孤立属性，而是标题在目录树中的相对位置。</b>
</p>

---

## ✨ 核心结论

在目录标题等级划分任务中，不建议让 LLM 直接判断：

```text
Development stage = H5
```

更推荐让 LLM 判断：

```text
Development stage.parent_index = 9.2 Design stage
```

然后由程序根据父子关系确定 Markdown 标题等级：

```text
9.2 Design stage 是 H4
Development stage 是它的直接子标题
所以 Development stage = H5
```

也就是说，最佳架构应当是：

```text
LLM 判断父子关系
        ↓
程序验证目录树合法性
        ↓
程序根据父节点确定标题等级
```

而不是：

```text
LLM 直接决定最终 H 等级
```

---

## 📌 问题背景

任务目标是：输入一段目录标题文本，让大模型判断每个标题的 Markdown 等级，例如 H2、H3、H4、H5。

但是在实验中发现，不同提问方式会显著影响结果：

| 提问方式       | 输出内容              | 稳定性     |
| -------------- | --------------------- | ---------- |
| 直接问标题等级 | `H2 / H3 / H4 / H5` | 容易不稳定 |
| 问父节点是谁   | `parent_index`      | 更稳定     |

原因在于：目录等级本质上是树结构关系，而不是单个标题文本本身的属性。

---

## 🧠 典型示例

例如目录片段为：

```text
9.2 Design stage
Development stage
```

如果直接让 LLM 判断等级，它需要同时完成两件事：

```text
1. 判断 Development stage 和哪些标题存在语义关系
2. 根据整个目录树推断它应该是 H4 还是 H5
```

这会把任务变成一个复杂的等级分类问题。

但如果让 LLM 返回父节点，它只需要回答一个更自然的问题：

```text
Development stage 属于哪个标题？
```

答案是：

```text
parent_index = 9.2 Design stage
```

随后程序可以确定性计算：

```text
9.2 Design stage = H4
Development stage 是 9.2 的直接子标题
所以 Development stage = H5
```

---

## 🔍 为什么 `parent_index` 更准确？

### 1. 父子关系比抽象等级更有语义

LLM 更容易理解语义归属关系：

```text
Index optimization 属于 Pre-retrieval techniques
```

但不一定稳定理解 Markdown 等级：

```text
Index optimization 应该是 H5
```

因为 H5 只是 Markdown 的表现形式，本身没有语义。
父子关系则是自然语言中更直接、更符合语义理解的问题。

---

### 2. 标题等级依赖全局结构

同一个标题在不同书籍、不同章节中可能对应完全不同的等级。

例如：

```text
Summary
```

它可能是：

| 场景     | 可能等级 |
| -------- | -------- |
| 全书总结 | H2       |
| 章总结   | H3       |
| 小节总结 | H4       |

只看标题文本无法判断等级。
但只要确定它属于哪个父节点，等级就可以自然推出。

---

### 3. 可以避免等级逐渐下沉

直接让 LLM 返回等级时，模型容易产生层级逐渐下沉的问题：

```text
H3 → H4 → H5 → H6
```

即使这些标题实际上是并列关系。

如果改为返回父节点，只要多个标题共享同一个父节点，程序就会自动赋予它们相同等级：

```text
A.parent = 9.2 Design stage
B.parent = 9.2 Design stage
C.parent = 9.2 Design stage

=> A / B / C 自动成为同级标题
```

这能显著减少目录树被错误拉深的问题。

---

### 4. `parent_index` 可以被结构规则校验

父节点输出可以被程序用结构约束检查。

例如：

```text
父节点必须出现在子节点之前
编号 3.2 通常属于第 3 章
多个并列标题可以共享同一个父节点
父子关系不能形成循环引用
父节点不能跨越明显不相关的 Part / Chapter 边界
```

如果模型直接输出：

```text
Development stage = H5
```

程序很难判断它为什么是 H5，也很难进行有效纠错。

但如果模型输出：

```text
Development stage.parent_index = 9.2 Design stage
```

程序就可以检查这个父节点是否合法。

---

### 5. 可以减少模型输出不一致

直接返回等级时，模型可能出现这种情况：

```text
Progression of RAG systems = H3
6.1 = H5
```

这说明模型可能理解了父子关系，但在等级编号上跳过了 H4。

如果改为返回父节点：

```text
6.1.parent = Progression of RAG systems
```

程序会必然生成：

```text
Progression of RAG systems = H3
6.1 = H4
```

这样可以把 LLM 的语义判断和程序的结构计算分离，避免模型在 Markdown 等级数字上出现不稳定。

---

### 6. 完整 Part 上下文同样关键

这次准确率提升不完全来自 `parent_index`。实验中同时做了两个改变：

| 方法   | 上下文范围       | 输出目标     |
| ------ | ---------------- | ------------ |
| 原方法 | 局部标题窗口     | 直接返回等级 |
| 新方法 | 完整 Part 上下文 | 返回父节点   |

完整 Part 上下文能让模型看到反复出现的结构模式，例如：

```text
3.2
  Advantages of chunking
  Chunking process

3.3
  What are embeddings?
  Embeddings use cases
```

当模型看到完整章节结构后，更容易判断这些无编号标题属于前面的编号标题，而不是错误地把它们提升或下沉到其他层级。

---

## 🧩 本质区别

### 直接预测等级

```text
标题 → H5
```

这是一个缺少解释的分类任务。

它的问题是：

```text
等级没有天然语义
等级依赖全局结构
输出结果难以校验
容易产生层级跳跃
容易把兄弟标题误判成父子标题
```

---

### 预测父节点

```text
子标题 → 父标题
目录树 → 自动计算等级
```

这是一个带语义依据和结构约束的关系推理任务。

它的优势是：

```text
父子关系更符合自然语言理解
父节点可以被程序校验
兄弟标题可以共享父节点
等级由程序确定性计算
目录树结构更稳定
```

---

## ⚙️ 推荐处理架构

```text
输入目录标题文本
        ↓
提供完整 Part / Chapter 上下文
        ↓
LLM 判断每个标题的 parent_index
        ↓
程序检查 parent_index 合法性
        ↓
构建目录树
        ↓
根据父节点深度计算 Markdown H 等级
        ↓
输出稳定标题等级结构
```

---

## ✅ 推荐输出格式

相比让模型输出：

```json
{
  "text": "Development stage",
  "level": 5
}
```

更推荐让模型输出：

```json
{
  "index": 17,
  "text": "Development stage",
  "parent_index": 16,
  "reason": "It is an unnumbered subsection under 9.2 Design stage."
}
```

然后程序计算：

```python
child.level = parent.level + 1
```

---

## 🧪 校验规则建议

程序可以对 `parent_index` 做以下检查：

| 校验项              | 说明                                                |
| ------------------- | --------------------------------------------------- |
| 顺序约束            | 父节点必须出现在子节点之前                          |
| 编号约束            | `3.2` 通常属于第 3 章，`3.2.1` 通常属于 `3.2` |
| Part / Chapter 边界 | 子标题不能随意跨越大的章节边界                      |
| 循环检测            | A 不能是 B 的父节点，同时 B 又是 A 的父节点         |
| 同级一致性          | 多个相邻标题如果共享父节点，应自动生成同级等级      |
| 最大深度限制        | 防止目录等级无限下沉到 H6 之后                      |
| 空父节点处理        | 顶级标题允许 `parent_index = null`                |

---

## 🚫 不推荐的提示方式

不推荐直接问：

```text
请判断每个标题是 H2、H3、H4 还是 H5。
```

因为这会让 LLM 同时承担：

```text
语义关系判断
全局结构推理
Markdown 等级计算
一致性校验
```

这些任务混在一起，容易导致输出不稳定。

---

## ✅ 推荐的提示方式

更推荐问：

```text
请判断每个标题的直接父标题是谁。
如果它是当前 Part / Chapter 下的顶层标题，则 parent_index 为 null。
不要直接判断 Markdown 等级。
```

可以进一步要求：

```text
1. 父标题必须出现在当前标题之前。
2. 优先依据编号结构判断父子关系。
3. 无编号标题通常属于最近的相关编号标题。
4. 多个并列标题可以共享同一个父标题。
5. 不允许形成循环父子关系。
```

---

## 🧭 最终结论

目录标题等级划分的关键，不是让 LLM 直接判断 H 等级，而是让 LLM 判断标题之间的父子关系。

原因是：

```text
H 等级是结构计算结果
父子关系才是语义判断结果
```

因此更稳健的设计是：

```text
LLM 负责语义归属：这个标题属于谁？
程序负责结构计算：它在目录树中是第几层？
```

这相当于把任务从：

```text
黑箱分类任务：标题 → H5
```

改造成：

```text
可解释关系任务：子标题 → 父标题 → 自动计算等级
```

所以在长文档标题标准化系统中，`parent_index` 通常比直接返回 `level` 更稳定、更可校验，也更适合工程化落地。
